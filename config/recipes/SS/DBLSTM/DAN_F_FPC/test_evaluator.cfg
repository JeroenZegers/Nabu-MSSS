[evaluator]
evaluator = multi_task
tasks = taskF taskP taskC
requested_utts = 3000
batch_size = 30
segment_length = full

[taskF]
evaluator = task_loss_evaluator
loss_type = deepattractornet
inputs = features
outputs = bin_emb

nodes = n0 bin_emb

n0_model = main
n0_inputs = features

bin_emb_model = outlayer
bin_emb_inputs = n0

features = testspecF
targets = partition_targets usedbins spectogram_targets mix_to_mask
partition_targets = testpartitiontargetsF
usedbins = testusedbinsF
spectogram_targets = testspectogramtargetsF
mix_to_mask = testmixtomaskF

[taskP]
evaluator = task_loss_evaluator
loss_type = deepattractornet
inputs = features
outputs = bin_emb

nodes = n0 bin_emb

n0_model = main
n0_inputs = features

bin_emb_model = outlayer
bin_emb_inputs = n0

features = testspecP
targets = partition_targets usedbins spectogram_targets mix_to_mask
partition_targets = testpartitiontargetsP
usedbins = testusedbinsP
spectogram_targets = testspectogramtargetsP
mix_to_mask = testmixtomaskP

[taskC]
evaluator = task_loss_evaluator
loss_type = deepattractornet
inputs = features
outputs = bin_emb

nodes = n0 bin_emb

n0_model = main
n0_inputs = features

bin_emb_model = outlayer
bin_emb_inputs = n0

features = testspecC
targets = partition_targets usedbins spectogram_targets mix_to_mask
partition_targets = testpartitiontargetsC
usedbins = testusedbinsC
spectogram_targets = testspectogramtargetsC
mix_to_mask = testmixtomaskC


