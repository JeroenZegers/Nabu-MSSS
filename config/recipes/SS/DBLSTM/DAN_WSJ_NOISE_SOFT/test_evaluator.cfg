[evaluator]
evaluator = multi_task
tasks = task0 task6 taskMin6
requested_utts = 3000
batch_size = 30
segment_length = full

[task0]
evaluator = task_loss_evaluator
loss_type = deepattractornetnoisesoft
inputs = features
outputs = bin_emb noise_filter

nodes = n0 bin_emb noise_filter

n0_model = main
n0_inputs = features

bin_emb_model = outlayer
bin_emb_inputs = n0

noise_filter_model = noisefilterlayer
noise_filter_inputs = n0

features = testspec0
targets = partition_targets spectogram_targets usedbins mix_to_mask
partition_targets = testpartitiontargets0
usedbins = testusedbins0
spectogram_targets = testspectogramtargets0
mix_to_mask = testmixtomask0

[task6]
evaluator = task_loss_evaluator
loss_type = deepattractornetnoisesoft
inputs = features
outputs = bin_emb noise_filter

nodes = n0 bin_emb noise_filter

n0_model = main
n0_inputs = features

bin_emb_model = outlayer
bin_emb_inputs = n0

noise_filter_model = noisefilterlayer
noise_filter_inputs = n0

features = testspec6
targets = partition_targets spectogram_targets usedbins mix_to_mask
partition_targets = testpartitiontargets6
usedbins = testusedbins6
spectogram_targets = testspectogramtargets6
mix_to_mask = testmixtomask6


[taskMin6]
evaluator = task_loss_evaluator
loss_type = deepattractornetnoisesoft
inputs = features
outputs = bin_emb noise_filter

nodes = n0 bin_emb noise_filter

n0_model = main
n0_inputs = features

bin_emb_model = outlayer
bin_emb_inputs = n0

noise_filter_model = noisefilterlayer
noise_filter_inputs = n0

features = testspecMin6
targets = partition_targets spectogram_targets usedbins mix_to_mask
partition_targets = testpartitiontargetsMin6
usedbins = testusedbinsMin6
spectogram_targets = testspectogramtargetsMin6
mix_to_mask = testmixtomaskMin6
