[evaluator]
evaluator = multi_task
tasks = taskZ taskA taskS
requested_utts = 3000
batch_size = 30
segment_length = full

[taskZ]
evaluator = task_loss_evaluator
loss_type = deepattractornet_softmax
inputs = features
outputs = bin_emb

nodes = n0 bin_emb

n0_model = main
n0_inputs = features

bin_emb_model = outlayer
bin_emb_inputs = n0

features = testspecZ
targets = partition_targets usedbins spectogram_targets mix_to_mask
partition_targets = testpartitiontargetsZ
usedbins = testusedbinsZ
spectogram_targets = testspectogramtargetsZ
mix_to_mask = testmixtomaskZ

[taskA]
evaluator = task_loss_evaluator
loss_type = deepattractornet_softmax
inputs = features
outputs = bin_emb

nodes = n0 bin_emb

n0_model = main
n0_inputs = features

bin_emb_model = outlayer
bin_emb_inputs = n0

features = testspecA
targets = partition_targets usedbins spectogram_targets mix_to_mask
partition_targets = testpartitiontargetsA
usedbins = testusedbinsA
spectogram_targets = testspectogramtargetsA
mix_to_mask = testmixtomaskA

[taskS]
evaluator = task_loss_evaluator
loss_type = deepattractornet_softmax
inputs = features
outputs = bin_emb

nodes = n0 bin_emb

n0_model = main
n0_inputs = features

bin_emb_model = outlayer
bin_emb_inputs = n0

features = testspecS
targets = partition_targets usedbins spectogram_targets mix_to_mask
partition_targets = testpartitiontargetsS
usedbins = testusedbinsS
spectogram_targets = testspectogramtargetsS
mix_to_mask = testmixtomaskS
